# 2026-05-06 大虾日记

## 任务：空闲重置 48 小时配置验证
- OpenClaw（我）：session.reset.idleMinutes=2880 ✅
- 满满：idle_minutes=2880 ✅
- 阿茶：idle_minutes=2880 ✅
- 小美：idle_minutes=2880 ✅
- 妙妙：idle_minutes=2880 ✅
- 当当：idle_minutes=1440（老大说不改）
- 小马（GenericAgent）：没有 session reset 机制，不需要改
- 所有 Hermes 进程启动时间早于配置修改时间，需要重启才能生效（尚未重启 Hermes）

## 小马 DingTalk runtime not initialized 问题排查（持续）
- 小马在钉钉反复报 "抱歉，处理请求时出错: DingTalk runtime not initialized"
- GenericAgent 代码和 dingtalk-stream SDK 中没有这个字符串
- GLM-5.1 via Tuyoo relay 返回 content:null + reasoning 有内容 → llmcore.py 已修：从 reasoning 提取内容当 content fallback
- 但杀掉小马进程后老大仍然收到同样报错 → 说明回复不是小马进程发出的
- **可能根因：钉钉机器人配了 HTTP 回调(outgoing webhook)，另一个服务在代答**
- 需要老大去钉钉开放平台确认机器人 dingpyoepw5vkoedesux 的消息接收方式配置

## 小马 GenericAgent 更新拉取
- git stash → git pull origin main → git stash pop，fast-forward 无冲突
- 20 个 commit 更新，关键：llmcore timeout/exception 修复、history folding、/resume 优化等
- 本地改动：sys_prompt.txt（自定义 system prompt）+ 3个 bat 启动脚本，均保留
- 小马已重启，钉钉 Stream 连接正常