# memory/2026-07-05

## Git 分类提交+清理+双git push
- workspace 清理54个临时文件（22个debug脚本、6张临时截图、4个bat、7个scripts/临时文件、temp目录等）
- 分类提交6个commit：docs(MEMORY/HEARTBEAT/TOOLS)、memory(6月日记)、data(todo-scan扫描数据)、data(dreams+git_check)、feat(PM系统)、feat(ima-skill)
- Tuyoo 内网 push 成功；GitHub 被 push protection 拦（DeepSeek key 在 memory/2026-04-26.md）
- 老大在 GitHub 允许了 secret push；同时我把文件里的 key 值也替换成脱敏版本
- 双 git 同步完成

## PMO早报排查
- WorkReportAssist_Morning 任务（09:00）从7月3号开始崩
- 根因：`work_report_assistant_config.json` 带 UTF-8 BOM（`\xef\xbb\xbf`），Python json.load() 不认
- 修复：去掉BOM，手动验证成功（今天周六正确跳过）
- BOM来源猜测：某个Windows编辑器或PowerShell Out-File写入时自动加了BOM

## 日志助手（MemoReminder）排查
- MemoReminder 任务（08:50/17:30）从6月13号开始断推送
- 6月12号17:30是最后一次成功推送
- 根因：`digest_config.json` 里 pm_system_url 配了 `http://127.0.0.1:8112`，但 PM 后端实际跑在 8000
- 修复：改成 8000，手动验证成功（PM calendar API 正常返回）
- 老大说从6月11号开始没推送，实际日志显示6月13号才断（6月12号还有一次17:30成功）

## EveningDigest 误启用
- EveningDigest_1800 任务（18:00晚间摘要）从4月25号后就没运行
- 老大确认这个任务已废弃，不需要修
- 排查过程中不小心给启用了，待老大确认是否要禁掉

## ToDo Scanner 状态清空
- 老大要求清空 todo-scanner 的记录
- state.json 里12条旧待办全部清除（最早6月22号，全部 dismissed/reminded 状态）
- shared-memory 里的历史归档保留（有参考价值）

## 踩坑：UTF-8 BOM 导致 JSON 解析失败
- 坑：Windows 编辑器/PowerShell 写 JSON 文件可能自动加 UTF-8 BOM，Python json.load() 不认 BOM 报 JSONDecodeError
- 绕法：用 Python 写文件时不加 BOM（open(path, 'w', encoding='utf-8') 默认无BOM）；检查已有文件用 `open(path, 'rb')` 读前3字节判断
- 记到：TOOLS.md

## 踩坑：PM 系统端口配置不一致
- 坑：digest_config.json 配了 8112，PM 后端实际跑在 8000，导致所有依赖 PM calendar 的定时任务全部失败
- 绕法：统一配置到 8000（与 PM 后端 restart_backend.bat 一致）
- 记到：TOOLS.md
