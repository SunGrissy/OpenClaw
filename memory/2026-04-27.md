# 2026-04-27 大虾日记

## 踩坑：钉钉群聊引用消息结构
- 坑：钉钉 Stream API 群聊引用消息的 isReplyMsg/repliedMsg 不在 text 顶层，藏在 text.extensions 里；私聊也一样
- 绕法：检查 text.extensions.isReplyMsg / text.extensions.repliedMsg 路径
- 记到：TOOLS.md

## 完成任务：群聊引用消息读取支持
- 问题：OpenClaw 和 Hermes Agent 在群聊里读不到用户引用的消息内容
- 根因：钉钉 Stream API 下发引用消息数据在 text.extensions 字段内，而非 text 顶层；两端代码都只检查了 text.isReplyMsg（顶层），遗漏了 extensions 路径
- 验证方法：在阿茶 gateway 加 AgentXia-QuoteDebug 调试日志，实测群聊+私聊引用消息
- 实测数据：
  - 群聊：conversationType=2, text.extensions.isReplyMsg=True, text.extensions.repliedMsg 含完整数据
  - 私聊：conversationType=1, 结构完全一致，也在 text.extensions 里
- 修改范围：
  1. D:\hermes\hermes-agent\gateway\platforms\dingtalk.py - _extract_text 方法，提取 text.extensions 中的引用消息
  2. D:\OpenClaw\extensions\dingtalk-connector\src\core\message-handler.ts - extractMessageContent 的 text/richText/reply 三个 case，同时检查 text.extensions 路径
- 回退方式：Hermes 用 git checkout；OpenClaw 用备份文件 message-handler.ts.bak.20260427
- 验证：妙妙(Hermes) + 小马(OpenClaw) 均已通过
- 清理：调试日志和 [AgentXia Task] 标记已全部删除

## 规范沉淀：代码修改标记规范 §7.1
- 位置：D:/MyAgents/shared-memory/legion-charter.md §7.1
- 内容：任何 Agent 修改非自身工作区代码时，必须加 [Agent{代号} Task] 标记
- 代号：虾叔=Xia, 满满=Man, 阿茶=Cha, 小美=Mei, 妙妙=Miao, 小马=Ma
- AGENTS.md 已加引用指向 §7.1

## OpenClaw 流式体验优化调研
- 问题：老大反馈 OpenClaw 在钉钉里"卡啊卡然后一大堆消息"，工具调用期间完全静默
- 现状：dingtalk-connector 已有 AI Card 流式文字输出，但工具调用期间无进度提示
- 原生支持：OpenClaw tool-progress preview 只支持 Discord/Slack/Telegram/Mattermost，不支持钉钉
- 可行方案：自研——在 reply-dispatcher.ts 的 onPartialReply 外加工具调用中间状态推送，类似 Hermes 的 tool_progress:all + interim_assistant_messages:true
- 状态：方案已和老大对齐，暂不实施，后续再排

## 踩坑：Hermes venv 重启不加载源码改动
- 坑：改了 D:\hermes\hermes-agent\gateway\platforms\dingtalk.py 源码后重启阿茶，新日志没出来
- 原因：Hermes 从 venv\Scripts\hermes.exe 启动，是 pip install 安装的，但实际路径确认是指向源码目录
- 绕法：删除 __pycache__ 缓存（del /s *.pyc），然后重启才生效
- 记到：TOOLS.md

## 踩坑：read 工具对大文件无限返回前几行
- 坑：对 message-handler.ts（1700+行）用 read 工具，无论 offset 设什么都返回开头几行
- 绕法：用 py -c 脚本读文件 + findstr /n 定位行号
- 这个之前 TOOLS.md 已记录，再次确认仍存在

## Hermes dingtalk.py 语法错误修复
- 坑：用 edit 工具写入 Python 代码时 \n 被当成字面字符而非换行符
- 绕法：用 write 工具写入完整函数，确保换行符正确；或用 py -c 验证语法后再提交
