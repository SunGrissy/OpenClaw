# 2026-06-25 每日记忆

## 昨夜/今晨
- 日期：6月25日周四，工作日
- 安静时段 23:00-08:00 无打扰

## 小马重启（已完成 ✅）
- 小马从6/20挂到6/25（5天）
- **根因链**：
  1. 锁端口19533被Windows内核ghost bind（WinError 10013，netstat不显示）
  2. ensure_single_instance锁端口失败 → 进程exit(1)
  3. start_dingtalk_daemon.bat有`:loop`循环，检测到退出5秒后拉起新实例 → 无限循环
  4. 禁用锁机制后反而导致6个进程同时抢钉钉连接（雪崩）
- **最终修复**：
  1. 加了ensure_single_instance_file()文件锁函数到chatapp_common.py（用文件锁替代socket锁，彻底绕过ghost bind）
  2. dingtalkapp.py改用文件锁（import加了ensure_single_instance_file）
  3. 杀掉start_dingtalk_daemon.bat（PID 66212/7476）
  4. 杀掉所有多余dingtalkapp进程
  5. 干净启动小马（PID 50940，16:15连接钉钉成功，文件锁生效）
- ⚠️ start_dingtalk_daemon.bat需要修改：去掉`:loop`循环或加锁检查，否则下次还会雪崩
- ⚠️ DingTalk-Daemon计划任务（dingtalk-desktop的daemon.py）也在跑，但不是小马重启的元凶
- ⚠️ 19533端口ghost bind仍未解决（管理员权限才能查netsh excludedportrange）

## 踩坑：Windows ghost bind + 多进程雪崩 + daemon.bat循环
- 坑1：19533端口bind失败（WinError 10013权限不足），但netstat不显示任何进程占用，SO_REUSEADDR也失败。不是TIME_WAIT（那是10048），是内核级ghost占用
- 坑2：禁用ensure_single_instance锁机制 → daemon.bat反复拉起 → 6个进程雪崩抢钉钉连接
- 坑3：start_dingtalk_daemon.bat有`:loop`循环，进程退出后5秒自动拉起，导致无限重启循环
- 绕法：① 用文件锁替代socket锁（彻底绕过ghost bind）；② 杀掉daemon.bat；③ 用DETACHED_PROCESS方式干净启动
- 记到：TOOLS.md
