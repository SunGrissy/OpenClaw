# 2026-04-25 工作记录

## 身份变更
- 老大给我定了新身份：**保安虾叔**，退休老教授转行当保安，扫地僧风格
- 更新了 IDENTITY.md / SOUL.md / MEMORY.md
- 称呼关系：我喊她老大，她喊我大叔

## Hermes 硅基军团排查与启动
- 老大说"Hermes 没干活"，查进程发现没跑，用 quick_start.bat 启动了全部服务
- 查了 Hermes 主 profile 配置：kimi-k2.6 模型，concise 人格，SOUL.md 全是技术铁律没角色身份
- Hermes 子 profile 有 pm/hr/design 三个，SOUL.md 都是默认模板

## 总管满满 = Director 新名字
- 老大确认"总管满满"是 Director 的新名字
- 当前 quick_start.bat 启动的是主 profile（HERMES_HOME=D:\hermes），不是 Director 专用
- 主 profile 需要正名为总管满满（SOUL.md 待改）

## 小美（设计 Advisor）上线
- 代码和 .env 都有，凭证验证有效（errcode:0）
- 首次启动 401 鉴权失败 → 环境变量覆盖 .env 的经典坑
- 写了 start_clean.bat（清除 DINGTALK_CLIENT_ID 等环境变量后再启动）
- 8302 端口被旧进程占 → taskkill 后重启成功
- 最终 stream_connected: True，可以 @小美 对话
- 小美目前是硬编码模板回复，未接 LLM

## 踩坑：read 工具对大文件无限循环
- 坑：对 org-structure.html（660+行）调用 read 工具时，无论 offset 设多少都返回前几行，陷入无限循环，反复调用了几十次同一个结果
- 绕法：发现 read 2-3 次返回相同内容就立刻换用 `py -c` + Python 脚本读写文件，或者用 exec + findstr/grep 定位
- 根因：可能是文件编码（UTF-8 BOM）或行尾符导致 offset 计算异常，未深究
- 记到：TOOLS.md
- 文件：D:\MyAgents\performeval\org-structure.html

## 踩坑：dingtalk-stream ChatbotHandler.process() 参数类型
- 坑：process() 收到的是 CallbackMessage，不是 ChatbotMessage。直接访问 message.is_in_at_list / message.text.content 等属性会 AttributeError，被 raw_process 的 try-except 静默吞掉，群里表现为"已读不回"
- 绕法：第一行写 `message = ChatbotMessage.from_dict(callback_message.data)` 再用
- 记到：硅基军团 SKILL.md 里的坑5，小美 main.py 已修复，阿茶/妙妙如果也有同样写法需检查
- 文件：D:\MyAgents\silicon-legion\advisors\design\main.py
